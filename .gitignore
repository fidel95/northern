.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*
node_modules

# Claude Design handoff source material — not part of the deployed site
.DS_Store
.claude/
prototype-files-needed/
Prototype files needed-handoff.zip
northern-pines-website-build-prompt.md

# Wrangler (Cloudflare Workers) local dev artifacts
.wrangler/
.dev.vars

# Cached Census boundary download (tools/make-service-map.py --keep)
/build/
